#include <stdio.h>

int main()
{
    int num=123;
    int sum= 0; //o ing out accumulator 
    
    sum = sum +num; 
    sum = sum +num;
    sum = sum +num; 
    sum = sum +num;
    
    printf("%d\n\r",sum); // %d - data format, \n new line , \r - 
    
    return 0;
}